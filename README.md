Contextual Search based on RAG Agents


First install required libraries from requirements.txt:
```
pip install -r requirements.txt
```

Navigate to ```web``` folder. Install the required packages:

```
npm install
```